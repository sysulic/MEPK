(define (domain conformant0)

  (:objects
  )

  (:agents
    a
  )

  (:predicates (P) (Q)
  )

  (:action split_kb
   :category (ontic)
   :parameters ()
   :precondition (True)
   :effect (<{(Q)} {(P)}>
    )
  )
  

  (:init (True)
  )

  (:constraint (True)
  )

  (:goal (or (!(Q)) 
             (and (Q) (P)))
  )

)
